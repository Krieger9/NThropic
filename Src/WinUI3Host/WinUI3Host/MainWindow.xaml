<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WinUI3Host.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI3Host"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:WinUI3Host.ViewModels"
    xmlns:messages="using:ClaudeApi.Messages"
    xmlns:controls="using:WinUI3Host.Views.Controls"
    mc:Ignorable="d">

    <Grid x:Name="rootGrid">
        <Grid.Resources>
            <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <ResourceDictionary Source="Templates/Templates.xaml"/>
                </ResourceDictionary.MergedDictionaries>
            </ResourceDictionary>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Replace ListView with MessageListControl -->
        <controls:MessageListControl Messages="{x:Bind ViewModel.Messages, Mode=OneWay}" Grid.Row="0" Margin="10"/>

        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Text="{x:Bind ViewModel.MessageText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     PlaceholderText="Type your message here..." Grid.Column="0"/>
            <Button Content="Send" Grid.Column="1" Margin="10,0,0,0" 
                    Command="{x:Bind ViewModel.SendMessageCommand}"/>
        </Grid>
    </Grid>
</Window>
