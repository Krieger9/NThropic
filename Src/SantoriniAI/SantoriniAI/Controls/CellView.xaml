<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="SantoriniAI.Controls.CellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SantoriniAI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid x:Name="RootGrid">
        <Viewbox Stretch="Uniform">
            <Grid Width="100" Height="100">
                <Image Source="{Binding BackgroundImage}" Stretch="Fill"/>
                <Image Source="{Binding DevelopmentImage}" Stretch="Fill"/>
                <Image Source="{Binding PawnImage}" 
                       Stretch="Uniform"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Top"
                       Width="20"
                       Height="20"/>
            </Grid>
        </Viewbox>
    </Grid>
</UserControl>